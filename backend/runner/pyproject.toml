[tool.pytest.ini_options]
# ".." 使共享包 backend/shared 可导入（shared.sandbox_kernel）
pythonpath = [".", ".."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]
