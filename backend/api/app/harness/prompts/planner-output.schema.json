{
  "type": "object",
  "required": ["intent", "loop", "delivery"],
  "additionalProperties": true,
  "properties": {
    "intent": { "type": "string" },
    "loop": { "type": "string", "enum": ["chat", "react", "plan_solve"] },
    "delivery": { "type": "string", "enum": ["confirm", "clarify", "text", "action"] },
    "notes": { "type": "string" }
  }
}
