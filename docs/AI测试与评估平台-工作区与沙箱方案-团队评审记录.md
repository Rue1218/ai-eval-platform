# AI 测试与评估平台 — 工作区与沙箱方案 团队评审记录

> 评审日期：2026-09-07 | 状态：**终审通过（归档）**——A V0.4.1 / B V0.6（勘误同步），第三轮终审两条主要级已补丁关闭
> 修订产物：A《工作区与沙箱设计方案》→ V0.3 → V0.4 → V0.4.1；B《沙箱执行方案重设计》→ V0.5 → V0.6；评审记录归档于本文件。

## 7. P5 复审结果（2026-09-07 第二轮，三员并行）

| 复审员 | 结论 | 核销 | 新发现问题 |
| :--- | :--- | :--- | :--- |
| R1 自洽性 | 有条件通过 | 15 项（BLK-1~5/MAJ-1~10）全部 ✅ 实质落地，无仅声明 | 0 阻塞 / 0 主要 / 11 次要（编号与引用残留） |
| R2 代码事实 | 有条件通过 | 10 项论断：7 项属实、2 项措辞修正、1 项方向正确补注记 | 0 阻塞 / 1 P1 / 3 P2 / 2 P3 |
| R3 安全契约 | 有条件通过 | BLK-3/4/5、MAJ-9 落点代码逐点核验成立 | 0 阻塞 / 5 主要（M-R3-1~5）/ 次要若干 |

### 收尾清单（P5.1 文案与引用级，不阻塞已定稿方向）
1. B D7-4/D7-5 仍引用已废止编号「S6」→ 改引 G1/G5 或彼稿 F1/F3（R1-1）。
2. A §12-2 审批接线残留「B 分支推翻 A」过时参照 → 标注随 B V0.5 关闭（R1-2）。
3. B 附录 B-6 悬空（A §12 无承接项）→ 显式并入 A §12 或在 A §12 增列（R1-3）。
4. A §9.1/§6.6 内引「§11 演练/§9 压测」未加彼稿限定 → 补「彼稿」（R1-4）。
5. A §6.2 补「未知/缺失 mode 默认拒绝」双向混布句（R1-5/R2-P2-4）。
6. A 修订标记体系清理（B1/B2/B3/M1/M2/M4/MIN-* 与 R 系列并存）→ 统一为评审记录 §6 口径或去除代号只留内容（R1-6）。
7. A §9 补显式回归基线（api 854/worker 50，AGENTS V1.8）与 B D6-5「50+」口径修正（R1-7/R2-P3）。
8. A §8.2 契约组②措辞修正：`reason` 键已在白名单（值变不依赖扩键）；真正被剥离的是目标档位等**新键**（R2-P1）。
9. A §4.2/§7.3 注记「活跃回合判定 = 单副本内存租约（`_SESSION_TURNS`），多副本前仅覆盖本副本」（R2-P2/R3-次要2）。
10. A §4.2 固化「锁序表：workspace 行 → sessions 行 + 绑定创建先锁 W + 删除×绑定×purge×approve 并发矩阵」（R2-P2/R3-次要2）。
11. A §8.2 契约组扩展（M-R3-1/2/3/4）：① `approval_terminal` outcome 枚举扩展（voided/recovery_failed：值/事件序「终态先、error 后」/前端 approvalDone 分支/回放断言）入 API.md V1.75，resume 失败检测点 = approve 清卡前预检检查点存在性；② `tool_result` 结构化 denied 字段（`data.sandbox`）入 V1.75 契约 + 前端兼容断言，`tool_approval` 词汇表注册漂移从「顺带核对」升级为显式修复项（归 `_EMITTER_ONLY_KINDS`）；③ 引用修正：改写落点为 API.md §4.3 tool_approval 行 + §4.3.1 bash 行（§9 红线表无此句）；④ 「403」表述改「unauthorized 错误事件」；⑤ ApprovalCard 倒计时列为新增项；⑥ 双 tool_result「双条」改「N 条按 call_id 事件序收敛于最后一条」（含 N=3 用例）。
12. 配额补「卷级水位熔断」（共享卷剩余 < 阈值 → workspace-write 整体拒写）或演练 5.1 判据对齐；声明数据卷无内核级目录配额（M-R3-4）。
13. 文档级：数据卷目录配额事实声明、单命令洪水残余为有界（同 12）。

### 裁决点（P5.2，正文修订前需裁决）
1. **M-R3-5 注销数据层动作**：`sessions.workspace_id` FK + 「注销只删记录」+「resolve fail-closed」三者需选型——**推荐软删**（workspaces 行 `deleted_at` 标记，resolve 校验行态 → VALIDATION，目录转 legacy 语义不变）vs FK RESTRICT（有绑定即注销失败）vs FK `ON DELETE SET NULL`（静默回落 legacy，违背 fail-closed，否决）。
2. **P4-§12-6 目录布局**：正文已按同根定稿——建议**直接采纳同根**（守卫已在案）；若改分流须在 F1 实施前裁决并出返工清单。
3. **P4-§12-4 DELETE 语义**：正文「注销默认保留 + purge 二次确认」与裁决点 1 耦合，随 F1 一并定稿。

其余 P4（name 唯一、运行中换绑、B-1 PoC、B-3 并发定标、B-6 层级规则）可延后，不阻塞本文定稿；跨副本回合态升级条件在 AGENTS/G6 标注。

## 8. 收尾执行记录（2026-09-07，A V0.4 / B V0.6）

**裁决点落地**：
- M-R3-5（注销数据层）：**采纳软删**——`workspaces.deleted_at` 标记；FK `ON DELETE RESTRICT` 作 purge 兜底（`SET NULL` 否决：静默回落违背 fail-closed）。落点：A §4.1/§4.2/§5。
- P4-§12-6（目录布局）：**采纳同根 + 孤儿守卫**；改分流需新评审。落点：A §12-6 关闭。
- P4-§12-4（DELETE）：注销软删定稿；剩余 purge 触发权限随 F1 定。落点：A §12-4。

**P5.1 收尾核销（13 项全部执行）**：
1. B D7-4/D7-5 废止 S6 引用 → G1/G5（B V0.6）✅
2. A §12-2 审批接线参照物关闭标注 ✅
3. B 附录 B-6 承接 → A §12-7 显式化 ✅
4. A §9.1/§6.6 彼稿引用限定 ✅
5. A §6.2 双向混布（未知/缺失 mode 默认拒绝）✅
6. A 头部修订标记体系声明（代号 = 评审记录追溯锚点）✅
7. A §9 回归基线显式化（api 854/worker 50 passed）+ B D6-5 口径 ✅
8. A §8.2 契约组② reason 措辞修正（键在 allowed，值变；新键才剥离）✅
9. A §4.2 回合租约单副本内存注记 ✅
10. A §4.2 锁序表（workspace → sessions）+ 删除×绑定×purge×approve 并发矩阵 ✅
11. 契约组扩展：outcome（voided/recovery_failed）+ 清卡前预检 + 事件序（终态先/error 后）+ tool_result 结构化 denied + tool_approval 词汇表注册显式修复 + ApprovalCard 倒计时新增 + 403 → unauthorized 措辞 + N 条收敛 ✅
12. §6.6 卷级水位熔断（`sandbox_volume_watermark_bytes`）+ 演练 5.1 判据对齐 ✅
13. 数据卷无内核级目录配额事实声明 + 文件工具/bash 双路径检查落点 ✅

**归档基线**：A V0.4.1（定稿候选，F1–F5/G1–G6 为实施真源）；B V0.6（责任面 = S2/S3 执行体侧 + D6 DoD）；本文档关闭（重新开放需新评审单）。

## 9. 第三轮终审记录（2026-09-07，A V0.4.1 / B V0.6）

| 终审员 | 结论 | 核销 | 新发现问题 |
| :--- | :--- | :--- | :--- |
| R1 自洽性 | ✅ 通过（建议顺手修订） | 裁决 3 + 收尾 13 = **16/16 实质落地** | 0 阻塞 / 0 主要 / 4 次要 + 3 极次要（版本引用/代号可追溯性/元注释/演练判据跨稿同步） |
| R2 代码事实 | 有条件通过 | 10 项论断全部核实成立（软删模型、回合租约单副本、锁面单行、reason 键白名单、outcome 枚举与前端 no-op、resume 仅记日志、词汇表漂移含运行时后果、新键无冲突、结构化透传、854 基线） | 0 阻塞 / 0 主要 / 3 MIN / 4 INFO |
| R3 安全契约 | 有条件通过 | V0.4 增量论断 F1–F11 基本属实 | 0 阻塞 / **2 主要（M-R3-6 软删组合、M-R3-7 作废终态）** / 7 次要 |

**M-R3-6（软删组合，A V0.4.1 补丁关闭）**：purge 被历史绑定（含软删会话，会话无物理删除）FK RESTRICT 永久阻挡且无解绑操作；孤儿守卫②不分行态致目录双路径滞留；导入 id 复用/复活未定义 → MAJ-1 续用未真闭合。关闭：孤儿判定与守卫②按行态（仅活跃行受保护，软删目录入清理面）；purge 事务内显式解绑（UPDATE + AuditLog `workspace_unbind`）再删；§7.2 导入 = 复活路径（清 deleted_at，旧会话自动恢复）；§9 口径对齐。

**M-R3-7（作废终态，A V0.4.1 补丁关闭）**：「预检失败不清卡」与单行互斥/终态语义矛盾（卡残留阻塞新卡与软删、/stop 双终态）。关闭：行锁内清卡与终态同事务（对齐 expired 先例），`voided` 终态 + error + AuditLog；§8.2③ 同步。

**次要项处置**：A §8.2 契约组并入 V1.75 内容清单（outcome/词汇表补录「非新增不升版」判定、卡归类成组与注释更新、`test_event_vocab` 断言补 `tool_approval`/`approval_terminal` 回归锁、`allowed_decisions` 文档-实现漂移校正、docstring 白名单补 `sandbox_scope`）；§6.6 熔断规格句（优先序/只挂写路径/文案）；陈旧 approval_id 用例入 §9；B §11-5.1 判据随彼稿同步（V0.6 勘误）。

**实施前置登记（随 F 系列 DoD 执行）**：① F1 建目录端点与孤儿守卫**同批合入**（守卫晚于建目录即存在误删窗口，R2-INFO-3）；② `sandbox_bash_mode` 与既有 `sandbox_engine` 键映射/改名决策在 F2 显式登记（防双键双真值，R2-MIN-3）；③ F3 范围含「解绑」动作形态与复活 UI（R2-INFO-2/R3-M-R3-6）；④ 词汇表补录与断言扩展随 F5 契约组（R2-MIN-2）。

**归档声明**：三轮评审（初评 → P1–P3 修订 → P5 复审 → 终审补丁）闭环；A V0.4.1 / B V0.6 为准予归档基线；本文档随归档关闭，任何后续变更（含 P4 遗留：name 唯一、运行中换绑、purge 触发权限、文件夹层级规则、B-1 PoC、B-3 定标）以新评审单重新开放。
> 评审对象：
> - A《AI测试与评估平台-工作区与沙箱设计方案》V0.2（主稿）
> - B《AI测试与评估平台-沙箱执行方案重设计》V0.4（关联稿）
> 评审团（并行只读评审，全部核对真实代码）：R1 自洽性与完整性 / R2 代码事实与可实施性 / R3 安全与契约。
> 代码基线：AGENTS.md V1.8（H5 批次 2 未完成项仍在册）。

## 1. 评审结论

| 对象 | R1 | R2 | R3 | 合并结论 |
| :--- | :--- | :--- | :--- | :--- |
| A（V0.2） | 有条件通过 | 有条件通过 | 有条件通过 | **有条件通过**：方向与代码现状吻合（sandbox_dir 唯一根、词表碎片、孤儿误判、userns root、卡存储结构均核实属实），须先关闭 5 阻塞 + 10 主要后放行 |
| B（V0.4） | 需修订 | — | — | **需修订**：头部声明 D7/D2/D3 已由 A 正式化，但正文 §2/§4.2/§5/§10/§12 仍保留旧语义（off/workfile/mutating、bash_verdict 白名单、逐命令审批、独占绑定）且无任何「已被 A 取代」标注——照 B 立项将按已删除的语义开发 |
| 跨稿 | — | — | — | 共享/独占并发模型两稿互斥且 A 正文与 A 待决点并存；目录拓扑基线（sessions/ vs workspaces/）两稿不一；档位命名两稿同名不同值 |

## 2. 阻塞级问题（放行前必须关闭）

| # | 位置 | 问题 | 修订要求 |
| :--- | :--- | :--- | :--- |
| BLK-1 | B 全稿 vs A | B 头部声明与正文脱节：D2/D3/D5/D6、§2 目标、§10/§12 仍按旧词表/白名单/逐命令审批语义，无取代标注；D7-2「独占绑定」被 A §5「属主内多会话共享」改变未声明；A §5 与 A §12-1 共享/待决并存 | B 逐节标注「已被 A §6 修订」或整体改写对齐；D7-2 修订登记；并发模型 A 正文与待决取一 |
| BLK-2 | A §6.3 / B D3（R2-M1） | 词表删除清单漏第四份：`feedback/rules.py` `BASH_BLOCK_PREFIXES` + `check_gates`（toolnode 每次 bash 先跑，早于 block/approval 分支），`test_harness_feedback.py` 门禁用例仍绿；仅删三处则「dispatch 不再静态裁决」无法达成 | 删除清单补 rules.py/check_gates 及其测试；「sudo 残余项」明确落点 |
| BLK-3 | A §6.4 / F5-G5（R3-B1） | 升档卡「跨重启可恢复、resume 至多一次」承诺不成立：PG checkpointer/粘性路由/重启恢复演练未完成（H5 批次 2），memory checkpointer 会丢恢复点/容量淘汰逐出中断回合 → approve 后静默丢失 | F5/G5 与 D6 硬前置补「H5 批次 2 完成 + 重启恢复演练通过」；未完成前降级表述为单副本语义并定义卡丢失审计终态（error + 卡作废），不得静默 |
| BLK-4 | A §5/§6.1.1/§6.4（R3-B2） | visibility=team 会话可绑定工作区 → 成员 B 可驱动写（授予档位时免审）或触发自动升档卡自批（owner 校验=回合发起者，非工作区属主），横向越权写 A 的 scope，与「不做跨用户共享」矛盾 | F3 起禁止 team 会话绑定（或绑定即强制 private）；升档审批授权主体=工作区属主/会话创建者（非属主 approve → 403）；补越权演练用例 |
| BLK-5 | A §6.3-3 vs §6.4（R1-主要3；R3-M4） | 「workspace-write 破坏命令由升档审批把关」失实——卡只在 read-only 拒写时触发，已授予写档的会话内 `rm -rf` 等无卡执行；且 tool_approval 从「事前审批」改「先试后升档重放」与 API.md §4.3.1「确认前不调用 Runner」字面冲突 | 承诺改述「档位授予（一次性）+ 拒写升档」；契约修订成组（API.md 升 V1.75 留档、ApprovalCard 文案/行为对照、卡 schema 白名单先扩再断言、`meta.schema_version` 评估） |

## 3. 主要级问题（定稿前修订）

| # | 位置 | 问题 | 修订要求 |
| :--- | :--- | :--- | :--- |
| MAJ-1 | A §5 M3 文案（R1-主要4） | 「失效后引导携带原 workspace_id 续用」断链：注销只删记录 → id 校验失败；目录转 legacy 无用户挂接端点 | 对齐为「数据保留至管理员清理前；续用需管理员介入/重建挂接入 F 系列」，或补同 owner 重建路径；purge 场景区分（数据已删） |
| MAJ-2 | A §10 F4/F5（R1-主要5） | 「稳定 7 天/任何一步异常」无量化判据 | 定义数值化判据集（EROFS 误报率/升档卡拒绝率/bash 报错率/逃逸告警上限）写入 DoD 与回滚段 |
| MAJ-3 | A §4.1 vs B §3.1（R1-主要6） | 现状目录拓扑两稿冲突（`data/sessions/<id>/work` vs `data/workspaces/<id>`，work 子层归属不清），「零迁移/逐字节一致」验收基线不可判定 | 统一现状拓扑口径（含 work 子层与 bind 关系）后再声明零迁移 |
| MAJ-4 | A §10 G 表（R1-主要2/次要3） | G1=F1+S2 但 S2 依赖 S1（落 G3）、S1 又依赖 S3（落 G2），依赖逆序；D6 计数 6 vs 7 不一致；api 基线 855 vs AGENTS 854 | G 表附 S↔F 映射与串行约束；计数与基线以 AGENTS V1.8 为准（854）并注明口径 |
| MAJ-5 | A §5/§6.2（R2-M2） | runner `is_valid_session_workspace` 仅收 root **直接子目录** + UUID 全匹配，嵌套 scope `{root}/<ws_uuid>/<folder>` 必被硬拒——runner 校验替换未列为工作项；新接口混布/回滚失败模式（新 api + 旧 runner）无兼容矩阵 | F2/F3 显式列入「runner 解析 policy 并替换该校验」；§9 补 runner 侧嵌套/符号链接用例；§10 补接口兼容策略；新请求体补 `max_output_chars` 字段与客户端同步 |
| MAJ-6 | A §4.4/§5（R2-M3） | 「会话创建对话框」前提不成立：前端新建会话 = 本地草稿（`resetToDraftSession`），首次发送才 `createSession`（title/visibility 两字段），无任何表单 | 补「草稿→发送」前绑定/档位配置交互设计；B §7「前端无改动」同步修订 |
| MAJ-7 | A §4.2/§7.3（R3-M1） | purge/清理守卫是请求时刻快照：与并发绑定创建、运行中回合存在窗口（快照后新建目录可删；rmtree 落在在途写入目录） | 删除路径对 workspaces/sessions 行 `FOR UPDATE` 并与绑定串行化；删除端点内集合复检；活跃回合二次判定；补并发回归用例 |
| MAJ-8 | A §6.1/§11（R3-M2） | workspace-write 常态化后无每工作区/会话磁盘配额 → 共享 data 卷可被写满（平台级 DoS） | 设计层承诺配额/容量告警（写超限档位拒写）；入 D6 硬门槛与压测；红队补「写满数据卷」用例 |
| MAJ-9 | A §6.4/§6.5（R3-M3） | ① EROFS stderr 特征字符串判定与「删词表（反对静态文本判定）」立场相悖；② 同一 call_id 双 tool_result（失败+成功）回放/断线合并语义未定义；③ resolve→bind 窗口符号链接换链 TOCTOU（共享写模型下） | denied 由 runner 结构化注入（/run 响应契约字段）；F5 明确双 tool_result 前端合并与回放断言；runner bind「realpath 后最终路径」；演练补换链用例 |
| MAJ-10 | A §8.1/§2.2（R3-M4③/S7） | 「WS/HTTP 协议零变化」与新增 `/api/workspaces/*` REST 端点、SessionCreate 增量字段矛盾 | 改写为「WS 事件 kind/上行零变化；REST 增量与新端点按 API.md 版本纪律经契约评审」 |

## 4. 次要级（随修订顺手关闭）

1. 修订标记 S 编号与 B 排期 S1–S7 撞号；A「S3 修订」标注缺失（§4.2 守卫段只标 B2）→ 修订号改 R 系列。
2. 「§7.3」引用错位（§7 为列表无编号小节）。
3. 档位键名 `sandbox.bash_mode` 无现有 config 点号先例（全扁平蛇形）且与 B `sandbox_mode` 同名不同值 → 统一命名并登记。
4. read-only 档表述过度：「全局不可写（仅 /dev/null）」——实际 `/tmp`/`/run`/`/dev` 为可写 tmpfs（HOME=/tmp）→ 口径改「scope/宿主不可写 + 沙箱临时区可写」，影响 §9 EROFS 用例选址。
5. B §3.1 残留 `data/sessions/…/work` 拓扑（并入 MAJ-3）；B 稿 `sandbox_kernel` 注释「批准后必须放行不得硬拒」与 runner L101-103 无条件硬拒现状矛盾（删清单时修正注释）。
6. 四份词表语义不等价（前缀 startswith vs 段切分正则；runner 拒集 ⊂ dispatch 拒集）→ 等价性矩阵补「前缀 vs 段切分」差异样例（`bash -c 'curl …'` 分层）。
7. 升档卡新增字段（reason=escalation/目标档位）不在 `_persist_pending_approval` allowed 白名单内（现状静默剥离）→ 先扩白名单再断言（并入 BLK-5 契约组）。
8. 自动升档卡受 `agent_approval_ttl_seconds`（默认 3600s）TTL 过期约束未说明 → §6.4 明示 + UI 倒计时。
9. 升档 approve 审计缺审批人身份 → AuditLog 补 action/user_id/workspace_id/call_id/thread_id（入 F5 DoD）。
10. `tool_approval`/`clarify` 未见 `PERSISTENT_KINDS` 注册的漂移核对（F5 契约留档时顺带补注册/声明）。
11. runner `/run` 收到未知/缺失 mode 默认拒绝（fail-closed）约定补入 §6.2。
12. admin「会话文件」页对绑定会话的显示口径（空/跳转工作区）未定义。
13. B 附录 B-1/B-3/B-6 待决点在 A §12 无承接；B-2 随 D2 修订自动失效未标注；A §12-6 独立子树若通过将连带改写 §4.2/§7/§9/F1（返工面未说明）。
14. A §9「三态」列 4 项笔误；「. .`/`..」排版；approve 重放后再失败（卡已清）回合语义未描述。

## 5. 修订行动清单（建议批次）

| 批次 | 范围 | 内容 |
| :--- | :--- | :--- |
| P1 | A → V0.3 | BLK-2 词表清单补全（含 sudo 落点）、BLK-3 升档硬前置与单副本表述、BLK-4 team 越权封闭、BLK-5 契约组（API V1.75 清单）、BLK-1 并发模型正文定稿 |
| P2 | A → V0.3 | MAJ-1~MAJ-10（失效续用、量化判据、拓扑口径、G 图、runner 校验替换、前端交互设计、删除竞态、磁盘配额、结构化 denied/双 tool_result/TOCTOU、REST 措辞） |
| P3 | B → V0.5 | 与 A 对齐：逐节取代标注或改写（D2/D3/D5/D6/§2/§6/§10/§12）、D7-2 修订登记、拓扑统一、命名统一、基线 854、DoD 计数 7（并入 H5 批次 2） |
| P4 | 开放待决 | 共享并发模型（A §12-1，随 BLK-1 定稿）、工作区目录布局同根 vs 分流（A §12-6）、name 唯一性、DELETE 语义、运行中换绑、文件夹层级规则 |
| P5 | 团队复审 | P1–P3 完成后二次评审闭环（对照本记录逐条关闭） |

## 6. 修订关闭对照（P1–P3 已完成，2026-09-07）

| 项 | 落点 | 关闭 |
| :--- | :--- | :--- |
| BLK-1 并发模型互斥 | A §5（定稿属主内共享/跨用户禁止）+ A §12-1（标记定稿）+ B D7-2/V0.5 | ✅ |
| BLK-2 词表清单漏第四份 | A §6.3-1（四处清单 + sudo 残余落点 = `bash_block_reason` 一条）+ B D3 取代标注 + 测试承接 A §9 | ✅ |
| BLK-3 升档跨重启承诺 | A §6.4 跨重启语义段（F5 硬前置 H5 批次 2；单副本语义 + error/卡作废终态）+ B D6-7 段 | ✅ |
| BLK-4 team 越权 | A §5（绑定强制 private/sharing 守卫/审批主体=属主 403）+ A §9 team 用例 + B D7-3 | ✅ |
| BLK-5 把关承诺失实 + 契约组 | A §6.3-4 承诺改述 + A §6.4 标题口径 + A §8.2 契约组（API V1.75/白名单先扩/ApprovalCard） | ✅ |
| MAJ-1 失效续用断链 | A §5 失效段 + A §7.2 legacy 导入（F3 交付） | ✅ |
| MAJ-2 灰度无量化判据 | A §9.1 判据表 + B D6-4 引用 | ✅ |
| MAJ-3 拓扑冲突 | A §7.1 + B §3.1 勘误（`data/workspaces/<session_id>`，无中间子层） | ✅ |
| MAJ-4 路线图逆序/基线 | A §10 G1–G6 重排（S1 取消标注）+ 基线 854（B §10/D6） | ✅ |
| MAJ-5 runner 校验替换 | A §6.2（显式工作项 + 混布回滚）+ A §9 runner 用例 | ✅ |
| MAJ-6 前端草稿模式 | A §4.4 会话设置面板 + B §7 撤回「前端无改动」 | ✅ |
| MAJ-7 删除竞态 | A §4.2 删除守卫③ + A §9 竞态用例 | ✅ |
| MAJ-8 磁盘配额 | A §6.6 + B D6-7 + 演练 5.1 | ✅ |
| MAJ-9 denied/双 tool_result/TOCTOU | A §6.5 结构化注入 + A §6.4 双 tool_result/重放再失败 + A §5 runner bind realpath + 演练 3.2 | ✅ |
| MAJ-10 REST 措辞 | A §2.2/§8.1「WS 零变化」口径限定 | ✅ |
| 次要 1–14 | A：修订标记 R 系列化、§7 编号子节、键名 `sandbox_bash_mode`、read-only 临时区口径、REST 留档、TTL/审计/词汇表核对、admin 跳转、笔误修正；B：附录 B 状态更新、残余档名清理 | ✅ |

**仍开放（P4）**：工作区目录布局同根 vs 分流（A §12-6）、`name` 唯一性（A §12-3）、DELETE 语义（A §12-4）、运行中换绑（A §12-5）、文件夹层级规则（B 附录 B-6）、S3 PoC 风险（B 附录 B-1）、并发默认值定标（B 附录 B-3）。
